{
  "name": "server",
  "version": "1.0.0",
  "description": "backend part of fullstack banking app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install && npm install --prefix client && npm run build --prefix client"
  },
  "author": "Enrique Parra",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.4",
    "dotenv": "^10.0.0",
    "express": "^4.16.3",
    "mongodb": "^3.6.2"
  }
}
